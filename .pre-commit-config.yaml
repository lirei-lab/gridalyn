# Git hooks in two layers, each sized to what it costs.
#
#   pre-commit  ->  black + isort + flake8   (~1 s)   on every commit
#   pre-push    ->  mypy                     (~16 s)  before code leaves the machine
#
# mypy is 30x the cost of flake8 on this tree (15.6 s vs 0.5 s, both measured
# cold). Charging that to every commit is how a team learns to type
# `--no-verify` by reflex, and a hook everyone skips protects nothing.
#
# WHAT THIS IS NOT
#
# It is not CI, and it does not replace it. These hooks run on YOUR machine with
# YOUR environment -- and this repository has already been bitten by exactly that
# gap: a local pre-commit run reported black clean on a file that CI's black
# reformatted, same pinned 24.3.0. `git commit --no-verify` skips all of it.
# Treat these as a fast way to avoid a red pipeline, not as the gate.
#
# The gates live in .github/workflows/ci.yml: test, projects, typecheck (a mypy
# ratchet against a measured baseline) and lint.

default_install_hook_types: [pre-commit, pre-push]

repos:
  - repo: https://github.com/psf/black
    rev: 24.3.0
    hooks:
      - id: black
        language_version: python3.12
        args: [--line-length=88]
        additional_dependencies: [poetry]

  - repo: https://github.com/PyCQA/isort
    rev: 5.13.2
    hooks:
      - id: isort
        name: isort (python)
        args: [--profile=black, --line-length=88]
        additional_dependencies: [poetry]

  - repo: https://github.com/pycqa/flake8
    rev: 7.0.0
    hooks:
      - id: flake8
        additional_dependencies: [poetry, flake8-bugbear, flake8-docstrings]

  # Deliberately `repo: local` + `language: system`, so this runs the mypy that
  # is installed in the project environment -- the same one CI runs. A
  # mirrors-mypy hook builds its own isolated env, which cannot see the
  # project's third-party stubs and therefore reports a *different* number for
  # the same tree (roughly 126 against 145). Two versions, two truths, and a
  # hook that approves what the gate rejects.
  #
  # It is also a ratchet, not a pass/fail check. mypy follows imports, so
  # checking one edited file pulls in its whole closure: a clean file measured
  # 21 errors across 10 other files. A hook that fails on someone else's debt
  # is a hook people bypass.
  - repo: local
    hooks:
      - id: mypy-ratchet
        name: mypy (ratchet against .mypy-baseline)
        entry: python3 tools/mypy_ratchet.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
      - id: mypy-ratchet-projects
        # Same mechanism, a second target: study scripts under projects/ were
        # outside every type gate until 2026-08-18, when a shared helper's
        # signature changed and 9 of 10 call sites in ev_hosting_flex kept the
        # old type -- no test caught it (the study skips in CI), and mypy
        # named the exact line and mismatched type when checked directly. A
        # separate baseline (868, measured 2026-08-18) rather than one shared
        # with gridalyn/'s (133): the two backlogs are held to different
        # bars, and folding the numbers together would obscure which side a
        # regression came from.
        name: mypy (ratchet against .mypy-baseline-projects)
        entry: python3 tools/mypy_ratchet.py --target projects --baseline-file .mypy-baseline-projects
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
      - id: mypy-ratchet-twin
        # A third target, scoped to gridalyn/twin/ (23 errors, measured
        # 2026-08-19): the digital-twin/semantic layer is a distinct backlog
        # from the rest of gridalyn/, folded into the whole-SDK number
        # (133) with no way to see a twin-specific regression separately.
        name: mypy (ratchet against .mypy-baseline-twin)
        entry: python3 tools/mypy_ratchet.py --target gridalyn/twin --baseline-file .mypy-baseline-twin
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

ci:
  autofix_commit_msg: "style: Auto-format code and docs [skip ci]"
  autoupdate_commit_msg: "chore: Update pre-commit hooks [skip ci]"
  autofix_prs: true
  # Was `skip: []` under a comment claiming mypy was skipped -- the comment
  # described an intent the empty list never implemented. mypy is now genuinely
  # off the commit path (pre-push above) and off the CI lint job.
  skip: []
