[project]
name = "pilot_backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["gridalyn"]

[project.entry-points."gridalyn.extensions"]
pilot_backend = "pilot_backend"

[tool.setuptools]
py-modules = ["pilot_backend"]
