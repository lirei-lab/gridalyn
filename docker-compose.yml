services:
  docs:
    build:
      context: ..
      dockerfile: docs/Dockerfile
    container_name: gridalyn-docs
    command: mkdocs serve -f docs/mkdocs.yml -a 0.0.0.0:8090
    working_dir: /workspace
    ports:
      - "8090:8090"
    volumes:
      - ..:/workspace
    restart: unless-stopped